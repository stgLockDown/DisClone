# ============================================
# NEXUS CHAT — Environment Variables
# Copy to .env for local dev, or set in Railway dashboard
# ============================================

# Server
PORT=8080
NODE_ENV=development

# Database Configuration
# IMPORTANT: Use "postgres" for Railway deployment (SQLite may have native module issues)
# Use "sqlite" only for local development
DATABASE_TYPE=sqlite

# SQLite (local development only)
DATABASE_PATH=./server/nexus.db

# PostgreSQL (RECOMMENDED for Railway production)
# When DATABASE_TYPE=postgres, Railway automatically sets DATABASE_URL
# Add a PostgreSQL service in Railway and set DATABASE_TYPE=postgres
# DATABASE_URL=postgresql://user:pass@host:5432/nexus

# Authentication
JWT_SECRET=CHANGE-THIS-TO-A-RANDOM-64-CHAR-STRING
JWT_EXPIRES_IN=7d
BCRYPT_ROUNDS=12

# CORS (comma-separated allowed origins)
# Leave empty for * (all origins), or specify: https://yourdomain.com,https://another.com
ALLOWED_ORIGINS=

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100