[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm rebuild better-sqlite3 --build-from-source"]

[start]
cmd = "node test-output.js || echo 'Test failed' && node server/diagnose.js && node server/index.js"